- name: app setup
  ansible.builtin.import_role:
    name: common
    tasks_from: app-setup.yaml

# - name: upgrading openssl packages
#   ansible.builtin.dnf:
#   name: "{{item}}"
#   state: present
#   loop:
#   - openssl
#   - openssl-libs
#   - openssh
#   - openssh-server
#   - openssh-clients

- name: python setup
  ansible.builtin.include_role:
    name: common
    tasks_from: python.yaml
  
- name: system setup
  ansible.builtin.include_role:
    name: common
    tasks_from: system-setup.yaml

